# 🦞 小龙虾 App - 三丰云部署完整指南

## 第一步：注册三丰云

1. 访问 **https://www.serveoverseas.com/**
2. 点击右上角"注册"
3. 用邮箱或手机号注册（**不需要实名认证**）
4. 登录控制台

---

## 第二步：领取免费 VPS

1. 点击左侧菜单 **"免费云服务器"**
2. 点击"立即领取"
3. 选择配置（推荐默认即可）
4. 选择系统：**Ubuntu 20.04** 或 **Debian 11**
5. 点击"立即创建"
6. 等待约 **5-10 分钟**，系统会自动创建完成

---

## 第三步：获取服务器信息

创建完成后，在控制台查看：
- **服务器 IP**：类似 `103.x.x.x`
- **密码**：创建时设置的 root 密码

---

## 第四步：SSH 登录服务器

### Windows 用户：
打开 PowerShell 或 CMD，执行：
```bash
ssh root@你的服务器IP
```
输入密码登录

### macOS/Linux 用户：
打开终端，执行：
```bash
ssh root@你的服务器IP
```

---

## 第五步：一键部署

登录后，依次执行以下命令：

### 方法一：使用一键部署脚本（推荐）
```bash
cd /var/www
git clone https://github.com/liuliucomputer/xiaolongxia-app.git
cd xiaolongxia-app/output/deploy
chmod +x deploy.sh
bash deploy.sh
```

### 方法二：手动部署

**1. 安装 Node.js**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
node --version
```

**2. 安装 PostgreSQL**
```bash
apt-get install -y postgresql postgresql-contrib
systemctl start postgresql
systemctl enable postgresql
```

**3. 创建数据库**
```bash
sudo -u postgres psql
```
进入 PostgreSQL 后执行：
```sql
CREATE DATABASE xiaolongxia;
CREATE USER xiaolongxia WITH PASSWORD 'xiaolongxia123';
ALTER ROLE xiaolongxia SET client_encoding TO 'utf8';
ALTER ROLE xiaolongxia SET default_transaction_isolation TO 'read committed';
ALTER ROLE xiaolongxia SET timezone TO 'Asia/Shanghai';
GRANT ALL PRIVILEGES ON DATABASE xiaolongxia TO xiaolongxia;
\q
```

**4. 克隆代码**
```bash
cd /var/www
git clone https://github.com/liuliucomputer/xiaolongxia-app.git
cd xiaolongxia-app/output/deploy
```

**5. 安装依赖**
```bash
npm install
```

**6. 创建环境变量**
```bash
echo "DATABASE_URL=postgresql://xiaolongxia:xiaolongxia123@localhost:5432/xiaolongxia" > .env
echo "PORT=3000" >> .env
echo "JWT_SECRET=xiaolongxia_secret_$(date +%s)" >> .env
```

**7. 使用 PM2 运行**
```bash
npm install -g pm2
pm2 start server/index.js --name xiaolongxia
pm2 save
pm2 startup
```

**8. 开放端口**
```bash
ufw allow 3000/tcp
systemctl restart ufw
```

---

## 第六步：访问应用

在浏览器访问：
```
http://你的服务器IP:3000
```

**默认管理员账号**：
- 用户名：`admin`
- 密码：`liuliu`

---

## 第七步：绑定域名（可选）

如果你想用域名访问：
1. 购买域名（阿里云/腾讯云，约 ¥50/年）
2. 将域名 DNS 解析到服务器 IP
3. 安装 Nginx 反代（需要的话告诉我）

---

## 常用命令

```bash
# 查看服务状态
pm2 status

# 查看日志
pm2 logs xiaolongxia

# 重启服务
pm2 restart xiaolongxia

# 停止服务
pm2 stop xiaolongxia

# 启动服务
pm2 start xiaolongxia
```

---

## 注意事项

⚠️ **安全提醒**：
1. 部署完成后立即修改管理员密码
2. 建议使用 SSH 密钥登录，禁用密码登录
3. 定期更新系统：`apt-get update && apt-get upgrade -y`

💡 **性能优化**：
- 免费 VPS 配置：1核 1GB 内存
- 适合少量用户测试，高并发需要升级配置

---

有问题随时问我！
