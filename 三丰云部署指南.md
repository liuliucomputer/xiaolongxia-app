# 小龙虾 App - 三丰云部署指南

## 📋 准备工作

### 1. 注册三丰云账号
访问 https://www.serveoverseas.com/ 注册账号

### 2. 领取免费 VPS
- 选择 "免费云服务器"
- 配置：1核 1GB 内存，Linux 系统
- 等待审核通过（通常几分钟）

---

## 🚀 部署步骤

### 第一步：SSH 登录服务器
```bash
ssh root@你的服务器IP
```

### 第二步：安装 Node.js
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
node --version  # 确认版本
```

### 第三步：安装 PostgreSQL
```bash
apt-get install -y postgresql postgresql-contrib
systemctl start postgresql
systemctl enable postgresql
```

### 第四步：创建数据库
```bash
sudo -u postgres psql
```
在 PostgreSQL 提示符下执行：
```sql
CREATE DATABASE xiaolongxia;
CREATE USER xiaolongxia WITH PASSWORD 'your_password';
ALTER ROLE xiaolongxia SET client_encoding TO 'utf8';
ALTER ROLE xiaolongxia SET default_transaction_isolation TO 'read committed';
ALTER ROLE xiaolongxia SET timezone TO 'Asia/Shanghai';
GRANT ALL PRIVILEGES ON DATABASE xiaolongxia TO xiaolongxia;
\q
```

### 第五步：克隆代码
```bash
cd /var/www
git clone https://github.com/liuliucomputer/xiaolongxia-app.git
cd xiaolongxia-app/output/deploy
```

### 第六步：安装依赖
```bash
npm install
```

### 第七步：创建环境变量
```bash
echo "DATABASE_URL=postgresql://xiaolongxia:your_password@localhost:5432/xiaolongxia" > .env
echo "PORT=3000" >> .env
echo "JWT_SECRET=your_random_secret_key_change_this" >> .env
```

### 第八步：使用 PM2 运行
```bash
npm install -g pm2
pm2 start server/index.js --name xiaolongxia
pm2 save
pm2 startup
```

### 第九步：开放端口
```bash
ufw allow 3000/tcp
systemctl restart ufw
```

---

## ✅ 验证部署

访问：http://你的服务器IP:3000

或者绑定域名后访问。

---

## 🔧 常用命令

```bash
# 查看日志
pm2 logs xiaolongxia

# 重启服务
pm2 restart xiaolongxia

# 停止服务
pm2 stop xiaolongxia

# 查看状态
pm2 status
```
