# 小龙虾 App - 部署指南

## 项目结构
```
xiaolongxia/
├── server/           # 后端代码
├── public/           # 前端文件
├── data/             # SQLite数据库（自动创建）
├── node_modules/     # 依赖包
├── package.json
└── server.js        # 启动脚本
```

## 阿里云部署步骤

### 1. 上传项目到服务器
使用 scp 命令上传：
```bash
scp -r C:/Users/liuliucomputer/WorkBuddy/小龙虾/output/deploy root@你的服务器IP:/opt/xiaolongxia
```

### 2. 登录服务器安装依赖
```bash
ssh root@你的服务器IP
cd /opt/xiaolongxia
npm install --production
```

### 3. 创建系统服务（可选）
创建 `/etc/systemd/system/xiaolongxia.service`:
```ini
[Unit]
Description=小龙虾 AI App
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/xiaolongxia
ExecStart=/usr/bin/node server/index.js
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
systemctl enable xiaolongxia
systemctl start xiaolongxia
```

### 4. 配置防火墙
```bash
firewall-cmd --add-port=3000/tcp --permanent
firewall-cmd --reload
```

### 5. 访问地址
- 本地访问：http://服务器IP:3000
- 建议配置 Nginx 反向代理 + HTTPS

## 测试账号
- 管理员：admin / liuliu
- 注册后可创建普通用户

## AI接口配置
已预配置 Agnes AI：
- API Key: sk-FwaH511AsiLrNbMvPC6lnzZzDjLBDHNKvuAuAKzS5puLY8HO
- Base URL: https://apihub.agnes-ai.cn/v1
- 模型: agnes-2.5-flash

如需更换，编辑 `server/routes/conversations.js` 第10行。
